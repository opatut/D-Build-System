# DBS - D Build System

## What is this?

This is a build system for the [D Programming Language](http://dlang.org).

## How to use it?

Compile it with 

    dmd *.d -ofdbs

Then copy the executable `dbs` to your path or call it from your project root. Configure the targets in `DBuildFile`.
